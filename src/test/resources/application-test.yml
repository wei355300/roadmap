
spring:
  config:
    import: "secure-test.yml"

tapd:
  default_workspace_id: ${tapd.default_workspace_id}
  basic_auth_id: ${tapd.basic_auth_id}
  basic_auth_pwd: ${tapd.basic_auth_pwd}
  workspaces: ${workspaces}
